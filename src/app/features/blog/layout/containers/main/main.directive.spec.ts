import { BlogLayoutMainDirective } from './main.directive';

describe('BlogLayoutMainDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogLayoutMainDirective();
    expect(directive).toBeTruthy();
  });
});
