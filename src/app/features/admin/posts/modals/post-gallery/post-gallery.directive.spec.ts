import { AdminPostGalleryDirective } from './post-gallery.directive';

describe('AdminPostGalleryDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminPostGalleryDirective
    ();
    expect(directive).toBeTruthy();
  });
});
