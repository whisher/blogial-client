import { UiRoleDirective } from './role.directive';

describe('UnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new UiRoleDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
