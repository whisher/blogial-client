import { UiSubscribeDirective } from './subscribe.directive';

describe('UiSubscribeDirective', () => {
  it('should create an instance', () => {
    const directive = new UiSubscribeDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
