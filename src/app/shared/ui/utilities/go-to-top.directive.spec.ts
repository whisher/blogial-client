import { UiGoToTopDirective } from './go-to-top.directive';

describe('UiGoToTopDirective', () => {
  it('should create an instance', () => {
    const directive = new UiGoToTopDirective(null);
    expect(directive).toBeTruthy();
  });
});
