import { UiHeaderScrollDirective } from './header-scroll.directive';

describe('UiHeaderScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new UiHeaderScrollDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
